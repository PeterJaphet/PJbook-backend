export enum ROLES {
  READER = 'reader',
  AUTHOR = 'author',
  ADMIN = 'admin',
}

export enum BOOKTYPE {
  PRIVATE = 'private',
  PUBLIC = 'public',
}

export enum GENDER {
  MALE = 'male',
  FEMALE = 'female',
}
